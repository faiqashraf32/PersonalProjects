# splitting characters from a string entered by the user

entry = input("Give me some characters to separate >> ")
x = []
for char in entry:
    x.append(char)

# loop that checks if p is in x
z = {}
for p in (x):
    if p == "a":
        # we know a is one set
        print("A is inside\n")
        z[] = "A"
    if p == "b":
        print("B is inside\n")
    if p == "c":
        print("C is inside\n")
    if p == "d":
        print("D is inside\n")
    if p == "e":
        print("E is inside\n")
    if p == "f":
        print("F is inside\n")